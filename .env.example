# 백엔드 API base URL (context-path 포함 여부는 백엔드 설정에 맞춰 조정)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080

# 미연동 도메인 mock 유지 토글 (true: mock 반환 / false: 실제 API 호출)
NEXT_PUBLIC_USE_MOCK=true

NEXT_PUBLIC_APP_ENV=development
